<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ClientAppsIntegration.WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClientAppsIntegration.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Padding="5">
            <StackPanel Grid.Row="0" Orientation="Vertical">
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button x:Name="btnLoad" HorizontalAlignment="Left" VerticalAlignment="Center" Click="btnLoad_Click">
                        <TextBlock Margin="5,2">Load Weather</TextBlock>
                    </Button>
                    <ProgressBar x:Name="pbLoading" Width="100" Margin="5" IsIndeterminate="True" />
                </StackPanel>
                <CheckBox x:Name="chkForceError" Content="Force Error" Margin="0,2" />
            </StackPanel>
        </Border>

        <controls:DataGrid x:Name="dgWeather" Grid.Row="1" AutoGenerateColumns="True" />
    </Grid>
</Window>
